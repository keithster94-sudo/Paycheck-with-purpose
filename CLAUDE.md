# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**Paycheck with Purpose** is a personal finance / budgeting application. This CLAUDE.md will be updated as the codebase grows — it was created when the repository was still empty.

## Commands

> Update this section once the project is initialized with a framework and tooling.

| Task | Command |
|------|---------|
| Install dependencies | _(add once package manager is chosen)_ |
| Start dev server | _(add once framework is chosen)_ |
| Run tests | _(add once test framework is chosen)_ |
| Run a single test | _(add once test framework is chosen)_ |
| Lint | _(add once linter is configured)_ |
| Build for production | _(add once build tooling is set up)_ |

## Architecture

> Update this section once the project structure is established.

Document the high-level data flow, main modules, and any non-obvious design decisions here as the codebase evolves.

## Key Conventions

> Update this section once patterns emerge in the codebase.

Document things like: state management approach, API layer conventions, data models, folder structure rules, etc.
